TCP는 stream 프로토콜이다. UDP와 달리 byte가 흐르는 것 같은 인터페이스를 제공한다. 따라서 보낸 만큼(send()) 받을 수 있다(recv())는 보장이 없다.

현재 server2.cpp에서는 recv함수가 한 번만 호출된다.
TCP에서 recv()는 필요한 바이트 수만큼 읽을 때까지 반복해야 한다
따라서 recv 시 받아야 하는 만큼 recv를 반복하는 while문 추가가 필요하다